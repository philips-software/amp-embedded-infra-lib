= GapCentral
:source-highlighter: highlight.js
:highlightjs-languages: protobuf

`GapCentral` implements the BLE GAP central role: scanning, connecting, pairing/bonding, security configuration, and identity/privacy management. All asynchronous callbacks are delivered on `GapCentralResponse`.

== State Constraints

Unless a method's documentation specifies otherwise, all methods are allowed in all link-layer states. Calling a method outside its allowed states results in an assertion.

== Startup

The first `CurrentState(standby)` message signals that the service is initialized and ready to accept method calls. No `GapCentral` methods may be called before this initial callback.

*Recognising the Node startup boundary:* on serial transports the Echo channel runs on top of xref:Sesame.adoc[SESAME]. The App must wait for the SESAME `Init` / `InitResponse` handshake to complete before the Echo layer can carry any traffic; only after that does the Node emit its per-service startup callbacks (`CurrentState(standby)` for GapPeripheral / GapCentral, similar readiness callbacks for other services). Practically the recognisable "Node has started" signal from the App's perspective is: (1) the SESAME transport reports the peer window as open (Init exchange complete), followed by (2) the first `CurrentState(standby)` on the service the App wants to drive. On TCP transports (Echo directly over TCP) the analogous boundary is the TCP connection established callback followed by the same `CurrentState(standby)`.

Calling a method before the initial `CurrentState(standby)` callback is a contract violation and results in an assertion on the Node side. This can also happen if the Node resets unexpectedly (e.g. watchdog, power event) while the App is running: the Node re-issues its startup handshake, and any App method call issued before the App observes the new `CurrentState(standby)` will assert. Applications should treat every fresh `CurrentState(standby)` callback as a service-ready boundary and re-drive their initial configuration from that point.

== Interface

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_class.mmd[]
....

== State Diagram

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_state.mmd[]
....

== Per-Method Sequence Diagrams

=== GetState

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_GetState_sequence.mmd[]
....

=== Standby

*Allowed states:* standby, scanning, initiating, connected.

Terminates the current activity (scan, initiating connection, or connected link) and returns the Node to standby. Calling from standby still yields `CurrentState(standby)`.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_Standby_sequence.mmd[]
....

=== SetDeviceDiscoveryFilter

*Allowed states:* standby.

Fire-and-forget, no response callback. Only devices matching the filter are reported via `GapCentralResponse.DeviceDiscovered`. Filters are reset automatically whenever scanning stops (the Node transitions out of `scanning`), so the App normally does not need to clear the filter between scan sessions. An explicit `SetDeviceDiscoveryFilter(filter=none)` is idempotent and useful when a filter was set without ever starting a scan.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_SetDeviceDiscoveryFilter_sequence.mmd[]
....

=== StartDeviceDiscovery

*Allowed states:* standby.

*Pre-conditions:* optional `SetDeviceDiscoveryFilter` to restrict reported advertisers.

Starts scanning. The Node transitions to `scanning` and reports each matching advertisement via `DeviceDiscovered` until `Standby()` is called.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_StartDeviceDiscovery_sequence.mmd[]
....

=== Connect

*Allowed states:* standby.

*Pre-conditions:* optional `SetIdentityAddress` / `SetPrivacyMode` (own address type used on CONNECT_IND).

Initiates a connection to a peripheral at the specified address. The Node transitions `standby` -> `initiating`. If a bidirectional data exchange completes within `initiatingTimeoutInMs`, the Node transitions to `connected`; otherwise it returns to `standby`.

*`initiatingTimeoutInMs` guidance:*

* Zero means wait indefinitely (per proto).
* Minimum practical value: a few times the peripheral's advertising interval, so the central can plausibly overhear a CONNECT_IND slot. For a peripheral advertising at 100 ms, at least 300-500 ms.
* Recommended: 3000-10000 ms for interactive use cases (App is waiting on user input, so a timely failure is preferable to hanging).
* Long-running / power-sensitive: 30000 ms and above, or zero, for cases where the App is content to wait for the peripheral to come into range.
* Example uses 5000 ms.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_Connect_sequence.mmd[]
....

=== PairAndBond

*Allowed states:* connected.

*Pre-conditions:* optional `SetSecurityMode` and `SetIoCapabilities` (consumed at SMP start to select the pairing method).

Secures the connection to the currently connected peripheral. Two paths are possible:

* *No pre-existing bond* - runs the LESC SMP procedure (Just Works, Numeric Comparison, or Passkey Entry depending on IoCapabilities negotiation), enables LL encryption, and stores the resulting keys as a bond.
* *Pre-existing bond* - skips SMP and re-encrypts the link using the stored LTK.

If the peripheral sends a security request first, this procedure runs automatically without an explicit App call.

*Response sequences* (matching `Gap.proto` PairAndBond doc):

* Successful pairing with new bond: `AuthenticationRequired` (if applicable) -> `NumberOfBondsChanged` -> `PairingResult`.
* Successful re-encryption with existing bond: `NumberOfBondsChanged` -> `PairingResult`.
* Pairing failure: `AuthenticationRequired` (if applicable) -> `PairingResult(pairedSuccessfully=false)`.
* Peer-initiated disconnect during pairing: `PairingResult(pairedSuccessfully=false)` -> `CurrentState(standby)`.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_PairAndBond_sequence.mmd[]
....

=== SetSecurityMode

*Allowed states:* standby.

Fire-and-forget, no response callback. Should be called once during initialization before any connections are made.

*Security mode is a pairing-time requirement, not a Connect-time filter.* Setting a required security level does not prevent the underlying `Connect` from succeeding; it prevents *pairing* from completing with peers whose IoCapabilities cannot satisfy the required level. Observable sequence when the peripheral cannot meet the level:

1. `Connect(...)` -> `CurrentState(initiating)` -> `CurrentState(connected)` (the connection is established normally).
2. `PairAndBond()` (or a peer-initiated security request that auto-triggers PairAndBond) starts SMP negotiation.
3. Central detects the peer's IoCapabilities cannot satisfy the required MITM level.
4. `PairingResult(pairedSuccessfully=false, reason=authenticationRequirementsNotMet)`.
5. The link stays connected on SMP failure. The App typically calls `Standby()` (or the peer disconnects voluntarily on receiving the SMP Pairing Failed PDU), producing `CurrentState(standby)` via one of those paths - not automatic on pairing failure per se.

Verified across the vendor stacks: each surfaces an SMP-layer authentication-requirement failure that the Echo layer maps to `PairingResult(pairedSuccessfully=false, reason=authenticationRequirementsNotMet)`. `SetSecurityMode` configures the SMP authentication requirement only and takes effect during the pairing procedure, not at Connect time.

If the App wants to reject connections to capability-mismatched peers up-front, it must decide before calling `Connect` (e.g. by inspecting scan-response data or a prior bond entry). The Node cannot pre-empt the connection based on security mode.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_SetSecurityMode_sequence.mmd[]
....

=== SetIoCapabilities

*Allowed states:* standby.

Fire-and-forget, no response callback. Should be called once during initialization before any pairing takes place. The selected capability drives the pairing method during `PairAndBond` (Just Works, Passkey Entry, or Numeric Comparison).

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_SetIoCapabilities_sequence.mmd[]
....

=== NumericComparisonConfirm

*Allowed states:* connected.

*Pre-conditions:* prior `AuthenticationRequired(isNumericComparison=true)`. Must be called within the 30s SMP timeout (Core Spec v6.3, Vol 3, Part H, Section 3.4).

*Peer disconnect during the confirmation pause:* while the App is waiting on the user to compare the derived passkey, the SMP procedure is paused waiting on the local `NumericComparisonConfirm` call. If the peer disconnects during that pause (LL_TERMINATE_IND, LL supervision timeout, HCI-level disconnect), the App may or may not observe `PairingResult(pairedSuccessfully=false)` before `CurrentState(standby)` fires. This is not a proto imprecision - it reflects a real limitation at the BLE-stack layer: an actively-running SMP procedure typically surfaces a cancellation on disconnect, but a paused procedure has no canonical cancellation event because the stack was waiting on the local device (not the peer) when the disconnect arrived. Practical guidance: treat `CurrentState(standby)` as terminal for the ongoing pairing procedure, and do not block on `PairingResult` after receiving it.

The same behaviour applies to `AuthenticateWithPasskey` during Passkey Entry (see below).

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_NumericComparisonConfirm_sequence.mmd[]
....

=== AuthenticateWithPasskey

*Allowed states:* connected.

*Pre-conditions:* prior `AuthenticationRequired(isNumericComparison=false)` (Passkey Entry method). Must be called within the 30s SMP timeout (Core Spec v6.3, Vol 3, Part H, Section 3.4).

*Peer disconnect during the passkey-entry pause:* same race condition as `NumericComparisonConfirm` above. If the peer disconnects while the App is collecting the passkey from the user, `PairingResult` may or may not fire before `CurrentState(standby)`. Treat `CurrentState(standby)` as terminal.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_AuthenticateWithPasskey_sequence.mmd[]
....

=== NumericComparison Authentication

*Pre-conditions:* connected; `IoCapabilities` set to `displayYesNo` or `keyboardDisplay`.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_NumericComparison_sequence.mmd[]
....

=== Passkey Authentication

*Pre-conditions:* connected; `IoCapabilities` set for passkey entry (display and/or keyboard).

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_PasskeyAuthentication_sequence.mmd[]
....

=== ResolvePrivateAddress

*Allowed states:* standby, scanning, connected, initiating.

Given a resolvable private address (RPA), returns the identity address of the bonded peripheral whose IRK produces that RPA, or reports failure if the address is not an RPA or does not match any stored bond. **Works against the entire bond list without requiring an active connection** to the peripheral.

*Typical use case:* Identify whether a device seen while scanning (which may advertise using a rotating RPA rather than its identity address) is already known / bonded, so the App can decide whether to reconnect via `Connect(bond.identityAddress)` or ignore the discovery. See also the *Identify Bonded Device* cross-service flow, which uses this method to fold RPA resolution into a discovery sweep. Since `IsDeviceBonded` is deprecated, `ResolvePrivateAddress` is the recommended way to answer "is this scanned device already bonded?" without initiating a connection.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_ResolvePrivateAddress_sequence.mmd[]
....

=== GetBondList

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_GetBondList_sequence.mmd[]
....

=== RemoveAllBonds

*Allowed states:* standby.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_RemoveAllBonds_sequence.mmd[]
....

=== RemoveBondWithAddress

*Allowed states:* standby.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_RemoveBondWithAddress_sequence.mmd[]
....

=== SetIdentityAddress

*Allowed states:* standby.

Fire-and-forget, no response callback. Used when privacy mode is disabled. Does not set a resolvable private address (that is controller-generated when privacy is enabled).

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_SetIdentityAddress_sequence.mmd[]
....

=== SetPrivacyMode

*Allowed states:* standby.

Fire-and-forget, no response callback.

*Pre-conditions:* when enabling, call `SetIdentityAddress` first so future bonds capture the intended identity.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_SetPrivacyMode_sequence.mmd[]
....

=== IsDeviceBonded

*Deprecated.* Will be removed in a future release. Prefer `ResolvePrivateAddress` or `GetBondList` for bond queries.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_IsDeviceBonded_sequence.mmd[]
....

== Cross-Service Sequence Diagrams

=== Discovery and Connection

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_DiscoveryAndConnection_sequence.mmd[]
....

=== Discovery Timeout

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_DiscoveryTimeout_sequence.mmd[]
....

=== Enable Privacy

*Pre-conditions:* standby.

Configures the central to use resolvable private addresses (RPAs) for its own advertising / initiating.

Note the order of the two configuration calls: `SetIdentityAddress` first, `SetPrivacyMode(true)` second. Although `SetIdentityAddress`'s doc line "used when privacy mode is disabled" implies the identity address is dormant with privacy on, it is still used during bonding: the identity address is what the central distributes to peers in the SMP `Identity_Address_Information` PDU as part of key distribution. Bonded peers store our IRK against that identity address so that on future reconnections they can resolve our rotating RPAs back to the same identity. If `SetIdentityAddress` is skipped, the identity address defaults to whatever the HAL implementation chose at boot (typically a device-unique random address).

*Order rationale:* set the identity you want to be bonded under **before** enabling privacy so that any bond created afterwards captures the intended identity address, not the boot default.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_EnablePrivacy_sequence.mmd[]
....

=== Identify Bonded Device

Scan for peripherals using resolvable private addresses (RPAs) and identify which ones are already bonded via `ResolvePrivateAddress`.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_IdentifyBondedDevice_sequence.mmd[]
....

=== Selective Bond Removal

*Pre-conditions:* standby.

Enumerate stored bonds via `GetBondList` and remove a specific bond by identity address using `RemoveBondWithAddress`.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_SelectiveBondRemoval_sequence.mmd[]
....

=== Pairing Failure Reasons

Illustrates the different `PairingFailedReason` values reported via `PairingResult(pairedSuccessfully=false)` for typical failure paths.

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_PairingFailureReasons_sequence.mmd[]
....

== Examples

=== Discovery and Connection

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_DiscoveryAndConnection_sequence_example.mmd[]
....

=== Numeric Comparison Pairing

[mermaid]
....
include::partial$echoservices/gap_central/GapCentral_NumericComparison_sequence_example.mmd[]
....

== Reference

Service definition: link:{page-origin-url}/blob/{page-origin-refname}/services/ble/Gap.proto[services/ble/Gap.proto].

.View Gap.proto inline
[%collapsible]
====
[source,protobuf]
----
include::example$Gap.proto[]
----
====
