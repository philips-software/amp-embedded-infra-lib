if (TARGET_ARCH_X86)
    add_subdirectory(http_server)
    add_subdirectory(https_client)
    add_subdirectory(serial_net)
    add_subdirectory(serial_output)
    add_subdirectory(sntp_client)
endif()
