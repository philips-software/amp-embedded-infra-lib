ccola_package(services)

    ccola_subdirectories(
        tracer
        util
        network
        network_win
        network_bsd
        synchronous_util
        cucumber
    )

ccola_end_package()
